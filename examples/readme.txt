这是一个示例文本文件

当请求匹配到相应的规则时，服务器会读取并返回此文件的内容。

您可以在规则文件中使用"file"字段指定要返回的本地文件路径，
请注意给需要返回的文件类型设置正确的Content-Type。

支持的文件类型包括但不限于：
- 文本文件（.txt, .html, .css, .js等）
- 图片文件（.jpg, .png, .gif, .svg等）
- 音频文件（.mp3, .wav, .ogg等）
- 视频文件（.mp4, .webm等）
- 文档文件（.pdf, .doc等）